Difference between HTTP1.1 vs HTTP2 

HTTP stands for hypertext transfer protocol & it is used in client-server communication. By using HTTP user sends the request to the server & the server sends the response to the user. 

HTTP 1.1
HTTP/1.1 which was created in 1997, It enables client and server machines’ interaction 
Using GET/POST request a service to the server
server includes the required source within the HTML page in response to the initial GET request 
It uses requests resource Inlining for use getting multiple pages 
It works on the textual format.

HTTP 2
HTTP/2 which was created in 2015
Here, both machines can implement their flow controls instead of using the transport layer instead of using the transport layer  
multiplexes data streams utilizing the same (one) TCP connection 
HTTP/2 supports multiple simultaneous responses to the client’s initial GET request, the server provides the required resource along with the requested HTML page
it uses PUSH frame by server that collects all multiple pages 
It works on the binary protocol 


